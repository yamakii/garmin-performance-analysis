# Load environment variables from .env file
dotenv

# Auto-apply the Node version from .nvmrc (22) via nvm, so entering the repo
# bumps Node instead of leaving the nvm default. No-op when nvm is absent.
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm use >/dev/null 2>&1 || true
fi
